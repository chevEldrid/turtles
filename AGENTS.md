## GitHub Access

- MUST: use the GitHub MCP connection for GitHub operations whenever available.
- MUST NOT: use `gh` CLI or other GitHub access paths unless GitHub MCP is unavailable or the user explicitly asks for an alternative.
